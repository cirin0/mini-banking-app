# 🏦 Мінібанкінг: рахунки та перекази (Proxy Pattern)

## 📋 Опис проєкту

Мета проєкту — створити мінібанкінг-сервіс, який дозволяє користувачам:

- створювати банківські рахунки;
- переглядати баланс;
- здійснювати перекази між рахунками;
- вести історію операцій;
- керувати платіжними картками (створення, пошук за номером/рахунком/користувачем);
- проходити автентифікацію та авторизацію через JWT (access + refresh токени).

Додатково реалізується **патерн Proxy** для:

- контролю доступу до банківських операцій;
- логування запитів;
- імітації поведінки реального банківського API без підключення до зовнішнього сервісу.

---

## 🧩 Основні функціональні вимоги

### 1. Облікові записи користувачів

- Реєстрація нового користувача (email, пароль);
- Авторизація через JWT (login/logout/refresh), refresh-токен видається в httpOnly cookie;
- Перегляд профілю поточного користувача (JWT).

### 2. Банківські рахунки

- Створення рахунку (тип: поточний, депозитний);
- Перегляд списку рахунків користувача;
- Перегляд балансу конкретного рахунку;
- Поповнення рахунку (deposit) з базовою валідацією доступу.

### 3. Перекази

- Створення переказу між рахунками;
- Переказ за номером картки/ідентифікатором (з валідацією номера картки);
- Валідація доступних коштів;
- Історія переказів користувача та отримання списку транзакцій за карткою.

### 4. Платіжні картки

- Випуск картки для конкретного рахунку (вказання типу картки та платіжної системи);
- Отримання картки за ID або номером;
- Перегляд карток користувача та карток, прив'язаних до рахунку.

### 5. Proxy Layer

- Клас-проксі, який:
  - перевіряє автентифікацію перед виконанням операцій;
  - веде лог запитів та відповідей;
  - імітує поведінку зовнішнього банківського сервісу;
  - забезпечує додатковий рівень безпеки.

---

## ⚙️ Технічні вимоги

| Компонент                  | Технологія / Інструмент             |
| -------------------------- | ----------------------------------- |
| Back-end framework         | NestJS (v11)                        |
| Мова                       | TypeScript (v5)                     |
| База даних                 | PostgreSQL + Prisma                 |
| Архітектура                | Modular + Proxy Pattern             |
| Аутентифікація/Авторизація | Passport + JWT (access/refresh)     |
| Валідація DTO              | class-validator / class-transformer |
| Тестування                 | Jest                                |
| API документація           | Swagger (OpenAPI)                   |
| Лінтинг/Форматування       | ESLint + Prettier                   |

---

## 👥 Розподіл ролей

| Учасник                             | Відповідальність                                                                                                                                                                                                |
| ----------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Учасник 1 (Backend Core)**        | - Налаштування NestJS проєкту;<br>- Реалізація модулів: `Auth`, `User`, `Account`;<br>- Підключення БД (PostgreSQL + Prisma);<br>- JWT авторизація;<br>- Swagger документація.                                  |
| **Учасник 2 (Proxy & Transaction)** | - Реалізація патерну Proxy;<br>- Модуль `Card`, `Transaction` (створення, історія, перевірка карт);<br>- Реалізація логування операцій;<br>- Тестування Proxy-шару;<br>- Middleware для логування та валідації. |

---

## 🚀 Запуск проєкту

1) Встановити залежності

```bash
npm install
```

2) Налаштувати середовище `.env` (приклад):

```env
DATABASE_URL=postgresql://user:password@localhost:5432/minibanking

PORT=3000
NODE_ENV=development
API_PREFIX=api

JWT_TOKEN_SECRET=supersecretkey
JWT_REFRESH_TOKEN_SECRET=anothersecretkey
```

3) Ініціалізувати БД (Prisma)

```bash
npx prisma migrate dev --name init
npx prisma generate
```

4) Запустити застосунок

```bash
npm run start:dev
```

- Swagger: http://localhost:3000/api/docs
- API префікс: `/${API_PREFIX}` (за замовчуванням `/api`)
